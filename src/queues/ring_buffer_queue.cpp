/**
 * @file ring_buffer_queue.cpp
 * @brief Implementation of BoundedRingBufferMPMC (header-only)
 * 
 * This is a header-only implementation to ensure inline optimization.
 * The implementation is fully contained in ring_buffer_queue.h.
 */

// Implementation is header-only
